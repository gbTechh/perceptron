#Proyecto Perceptron

Integrantes:

- Rodrigo Silva
- Pamela Villar

#Ejecucion:

Tengo 2 archivos, el main.cpp tiene el perceptron hecho en c++ y para paralelizar use threads, me ayuda con el tiempo de ejecucion para hacer pruebas, se demora menos de 2 minutos para 20 épocas con el entrenamiento de 50000 del datasets y 10000 de testeo, estoy usando el perceptron simple con el que aprendimos en clase y obtengo entre 22-26% de aciertos

Luego el archivo perceptron.cu que esta hecho en cuda, el cual estaba en la implementacion de main.cpp pero en vez de usar threads use cuda. Tiene la misma lógica pero demora un poco más, no entendemos mucho aun como usar cuda y por qué la demora, aunque este solo lo ejecutamos en google colab, y el porcentaje es un poco menor (tampoco entendemos aun por que)
